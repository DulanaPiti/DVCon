# Source Files

This directory will contain all HDL source files organized by subsystem.

## Structure

- `core/` - Core FPGA accelerator modules
- `nn/` - Neural network layer implementations
- `utils/` - Utility modules and common functions
- `sim/` - Testbenches and simulation files
